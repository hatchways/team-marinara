import React from 'react';
import { Typography } from '@material-ui/core';

const Landing = () => (
  <div>
    <Typography variant="h1" align="center">Landing Page</Typography>
  </div>
);

export default Landing;