import React from "react";
import { Grid, Typography, makeStyles } from "@material-ui/core";

const Campaign = props => {
  return (
    <Grid item container>
      <Typography variant="h6">Individual Campaign</Typography>
    </Grid>
  );
};

export default Campaign;
